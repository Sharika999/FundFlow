pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("com.android.application") version "8.1.0" // Updated AGP version
        id("com.android.library") version "8.1.0"
        id("org.jetbrains.kotlin.android") version "1.9.0" // Ensure Kotlin is compatible
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "fundflow"
include(":app")

